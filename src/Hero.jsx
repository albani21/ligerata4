import "./Hero.css";

function Hero() {
  return (
    <section class="hero">
      <h1>Alban</h1>
      <p>Welcome — a small selection of projects below.</p>
    </section>
  );
}

export default Hero;
